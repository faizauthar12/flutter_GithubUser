import 'package:flutter/material.dart';
import 'package:github_user/components/GHUserBody/GHSearchResult.dart';

class GHUserBody extends StatelessWidget {
  const GHUserBody({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[],
    );
  }
}
